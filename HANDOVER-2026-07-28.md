# 인수인계 (2026-07-28) — 지진 알림 · Firestore 비용 · 자동채널 스캔

> 이 문서는 **2026-07-16 ~ 07-28 세션**의 작업·판단·실측 결과를 다음 세션이 이어받도록 정리한 것이다.
> 서비스 전반은 `HANDOVER.md`(2026-07-11), 규칙은 `CLAUDE.md`, Next.js 16 주의는 `AGENTS.md` 참고.
> **이 문서가 최신이다. 충돌하면 이 문서를 따른다.**

---

## 0. ⚠️ 다음 세션이 가장 먼저 알아야 할 것

1. **작업 후 반드시 `git push origin HEAD:master`** (사용자 지시, 개발단계 자동배포). develop 푸시는 안 한다.
2. **절대 지어내지 말 것.** 이번 세션 최대 실패가 "측정 없이 단정"이었다. 아래 §7 필독.
3. **자동화 브라우저로 영상 카드를 클릭하지 말 것.** 재생불가 오탐 신고 → 운영 마커가 영구 차단된다. (메모리 `no-video-card-click-in-automation`)
4. **Firestore 읽기가 다시 튀면** §3 의 "전체 스캔 소비처" 목록을 먼저 전수 확인. 한 곳만 고치면 반드시 재발했다(4회 반복).
5. **마커 조회의 기본 경로는 `markerIndex.js`(청크 인덱스)** 다. §2 참고.
6. 사용자는 **코딩을 모른다.** 전문용어 없이 설명하고, 확인이 필요하면 "무엇을 눌러 무엇이 보이는지" 체크리스트로 준다.

---

## 1. 이번 세션 커밋 (시간순, 전부 master 배포됨)

| 커밋 | 내용 |
|---|---|
| `9b565bf` | Firestore 읽기 baseline 절감 — 스냅샷 재계산 주기 연장 |
| `0ab626b` | 대시보드 사용량 집계를 **UTC 자정 → 태평양 자정**(실제 한도 리셋)으로 수정 |
| `3f9c9ad` | 구글 서치콘솔 소유권 확인 HTML 파일 추가 |
| `457fcbf` | 지역 소개글 단일 문서 조회 (SEO 페이지당 137~274 읽기 → 1) |
| `e59432f` | **마커 청크 인덱스 + 증분 갱신** (핵심 아키텍처 변경) |
| `f1bedbf` | 문의 페이지에 '영상 게시 중단(삭제) 요청' 안내 |
| `42abaf6` | 약관에 임베드 운영 4원칙 + 푸터에 게시중단 링크 |
| `06b1779` | 지진 레이어 상시 라벨에 발생시각·장소 추가 (2D/3D) |
| `a950668` | continent 없는 마커를 공개 목록에서 제외 ('unknown' 트리 제거) |
| `6904792` | **지진 발생 알림 팝업 + 지진 상세 SEO 페이지** |
| `e217586` | 지진 상세 페이지 한글+영어 병기 |
| `39a81ec` | 지진 알림 노출 규칙 변경 + 근접 영상 500km/1,000km 구간 |
| `0521001` | **자동채널 등록 실패 수정** (Gemini 503 재시도 누락 + maxDuration 부재) |
| `8806cbc` | **스캔 300초 → 48초** (Firestore 순차 왕복 제거 + 지역설명 AI도 503 재시도) |

---

## 2. 마커 조회 아키텍처 (가장 중요 — 건드리기 전에 반드시 읽을 것)

### 2-1. 청크 인덱스 (`src/lib/markerIndex.js`)
공개 마커 조회의 **기본 경로**. 마커 전체를 `marker_index` 컬렉션에 저장한다.

```
marker_index/meta        { chunk_count, total, last_sync_ms, last_full_ms }
marker_index/chunk_0..N  { markers: [...], count }   ← 각 600KB 이하
```

- **읽기가 마커 수와 무관하게 고정**(청크 문서 몇 개만 읽음). 마커 3만 개여도 동일.
- 갱신은 **증분**: 마지막 동기화 이후 `updated_at` 바뀐 문서만 조회(평소 0건 → 비용 ~0).
- **7일마다 자동 전체 재구축**(하드 삭제 등 증분이 놓치는 변화 정합화).
- 읽기 요청 시 `last_sync_ms` 가 60초보다 낡으면 증분 동기화를 먼저 수행.
- 공개필터(수동: `is_active!==false && !auto_disabled`, 자동: `is_live===true` + 좌표) 와
  **전역 중복제거**(같은 `youtube_video_id` 는 수동 우선)를 인덱스가 이미 적용해 저장.
- **안전장치**: 인덱스가 없거나 실패하면 기존 컬렉션 스캔으로 자동 폴백 → 무중단.

사용처: `getMapMarkers()`(홈·사이트맵·seo_nav 경유), `getCountry/ContinentPublicMarkers`.
마커 상세는 인덱스를 안 쓰고 문서 직접 조회(`getPublicMarkerById`)라 항상 최신.

### 2-2. `hasLocation` 필터 (`src/lib/hasLocation.js`)
`continent` 가 비어 있으면 = "AI가 장소를 특정 못함" → 공개 목록에서 제외.
**5곳에 중복 구현돼 있어 공용 함수로 통일**했다. 하나만 고치면 경로별 결과가 달라지니 반드시 함께 수정:
`markerIndex.js` / `queryPublicMarkers.js` / `getAutoMarkers.js` / `getPublicMarkers.js` / `getPublicMarkerById.js`

### 2-3. 시간제 스냅샷 (`src/lib/liveSnapshot.js`)
`getTimedSnapshot({docId, refreshMs, compute})` — 결과를 `live_snapshots/{id}` 에 저장하고 주기마다 1회만 재계산.
현재 사용: `broadcast`(1h), `iss`(1h), `seo_nav`(6h), `manual_video_ids`(3h), `sitemap_routes`(6h),
`country_<CC>`/`continent_<name>`(3h, 인덱스 실패 시 폴백용).

**⚠️ `unstable_cache` 는 Vercel 서버리스에서 인스턴스별로 분리돼 중복조회를 못 막는다.** 시간캐시가 필요하면 Firestore 스냅샷을 쓸 것.

---

## 3. Firestore 읽기 사고 이력 (같은 실수 4회 반복 — 필독)

7/13부터 읽기가 무료한도(5만/일)를 초과하기 시작해 최대 **35만/일**(7/16)까지 갔다.
원인은 **매번 같은 클래스**: "페이지 렌더마다 컬렉션 전체를 스캔". 일부만 고쳐서 4번 재발했다.

| 회차 | 놓친 곳 | 고침 |
|---|---|---|
| 1 | 마커/채널 **상세** 페이지가 전체조회 후 `.find()` | 단일 문서 조회 |
| 2 | 대륙/국가/도시 **목록** 페이지 본문 | 타겟 쿼리 |
| 3 | **공유 헤더**(`SeoPageShell`→`getSeoNav`), 중복제거, sitemap | 시간제 스냅샷 |
| 4 | 국가/대륙 **범위 쿼리** 자체 + 빌드 사전생성 | 청크 인덱스 + `generateStaticParams: []` |

**교훈(반드시 지킬 것):**
- 읽기 초과를 조사할 땐 **GCP Monitoring 을 시간대별 + read type별(QUERY vs LOOKUP)** 로 먼저 실측한다.
  밤새 꾸준하면 크롤러, 특정 시각 스파이크면 배포(빌드)다.
- `getNormalizedPublicMarkers()` / `getLiveChannels()` / 컬렉션 `.get()` 호출부를 **grep 으로 전수 조사**한 뒤 한 번에 고친다.
- "컬렉션 전체를 읽어 맵 만든 뒤 하나만 꺼내 쓰는" 패턴은 전부 단건 조회로 바꾼다.

**현재 상태(2026-07-28 기준)**: 스파이크 없이 하루 2~3만 수준. 한도 안.

---

## 4. 지진 알림 기능 (이번 세션 신규)

### 4-1. 팝업 (`src/components/EarthquakeAlert.js`)
- 🌍 지진 토글과 **무관하게 항상** 동작. 규모 **4.5 이상**이면 알림. 5분마다 `/api/earthquakes` 조회.
- 표시: 규모·장소·발생시각·깊이 + **진앙에서 가까운 라이브캠**(거리 표기) + 쓰나미 배지 + USGS PAGER 등급.
- 버튼: `지도에서 보기`(지진 위치 이동 + 지진 레이어 자동 ON) / `자세히 보기`(SEO 페이지) / 공유.
- 테두리: `.eq-alert-blink` (globals.css) 빨간 경고등 깜빡임. `prefers-reduced-motion` 시 정지.

**노출 규칙 (사용자 확정 사양):**
- **✕ 닫기 = 이번 화면에서만.** 기록을 **메모리(`dismissedRef`)에만** 둔다 → **새로고침·재접속하면 다시 보임**.
- **"오늘 하루 보지 않기"** 버튼 → `localStorage.livecam_eq_mute_until` 에 **24시간** 음소거(전체 알림).
- 규모별 유지기간(신규 접속자 기준): 4.5~5 **1일** / 5~6 **2일** / 6~7 **2.5일** / 7~8 **3일** / 8+ **5일**
- 4.5+ 는 하루 20~30건이라 **한 번에 1건(최대 규모)만** 띄우고, 닫으면 그 시점 대상들을 함께 처리.

### 4-2. 근접 라이브캠 구간 (사용자 확정 사양)
- **500km 이내** / **1,000km 이내** 두 구간으로 나눠 표시.
- **1,000km 초과는 제외** → 둘 다 없으면 "가까운 지역 영상 없음 (1,000km 이내)".
- 팝업과 SEO 상세 페이지 **양쪽 동일** 적용. 로직: `src/lib/earthquakeAlert.js` `groupNearestByDistance()`.

### 4-3. 지진 상세 SEO 페이지 (`src/app/earthquake/[eventId]/page.js`)
- 목적: 큰 지진 검색 유입. 팝업은 JS라 검색엔진이 못 읽으므로 **서버 렌더 페이지**를 별도로 만든 것.
- 데이터: USGS 이벤트 API 단건 조회(무료, 과거 지진도 조회 가능) → **페이지는 영구히 살아있다**.
- **한글+영어 병기** (이 페이지만 예외. 다른 SEO 페이지는 한국어 고정) — 지진은 해외 검색량이 압도적이라
  "Earthquake" 영어 키워드가 없으면 영어 검색에 전혀 안 걸리기 때문.
- sitemap 에는 USGS **"significant"(주요 지진) 월간 피드**만 등록(현재 12건 수준).
  규모 4.5+ 전체를 넣으면 얇은 페이지 대량 생성 → SEO 에 해롭다.
- ISR 1시간 캐시, `generateStaticParams: []`(빌드 사전생성 안 함).

---

## 5. 자동채널 스캔 (이번 세션 대수술)

### 5-1. 고친 것
1. **Gemini 503 재시도 누락** (`autoMarkerAi.js`, `regionDescriptionAi.js` **양쪽 다**)
   - 기존: **429만 재시도**, 5xx 는 즉시 throw → 배치 전체 실패 → `enrichFailed` → "위치를 특정하지 못했습니다"
   - 수정: `RETRYABLE_STATUS = {429,500,502,503,504}`, 5xx 는 지수 백오프(2s→4s→8s), 최대 4회
   - **이것이 "채널 등록이 안 된다"의 진짜 원인이었다.**
2. **등록 라우트 `maxDuration=60` 추가** (`api/auto-channels/route.js`) — 원래 없었다(내가 지운 게 아님).
3. **Firestore 순차 왕복 제거** (`autoMarkerScan.js`)
   - 기존: 라이브 영상마다 `await ref.get()` → `await ref.update()` (건당 61ms/55ms × 735개 ≈ **85초**)
   - 수정: 읽기 `getAll`(300개씩), 쓰기 `BulkWriter`. 종료표시·last_seen 갱신도 BulkWriter.
   - **실측 개선: 읽기 17.7배, 쓰기 23.1배**
4. **안내 문구 원인별 분리** (`AutoChannelForm.js`): 성공 / AI 일시오류(재시도 안내) / 위치 특정 불가 / **임베드 차단·방송 종료**

### 5-2. 실측 성능 (2026-07-28, 채널 241개)
```
스캔 전체: 300초 → 47.9초
scan 결과: liveVideoCount 745, reused 717, newEnriched 28, enrichFailed 0,
          markedEnded 18, candidateIdCount 4258, videosListUnits 86
```

**단계별 실측 (반복 측정 기준):**
| 단계 | 소요 |
|---|---|
| RSS + /streams 스크래핑 (241채널 동시) | **3.6초 / 99MB** (113개 전부 정상) |
| 채널별 마커 조회 (241쿼리) | 0.5초 |
| videos.list 라이브 판정 | 약 5초 |
| Gemini 배치 8개 1회 | **평균 4.6초** (5회 측정, 4.5~4.8) |

### 5-3. ⚠️ `scanChannels` 3단계 `getAll` 은 **없애지 말 것**
"1단계와 중복"으로 보이지만 **안전장치**다:
- 3단계는 **"이 영상의 마커가 이미 있는가/없는가"**를 판별한다. 1단계는 이미 있는 것만 훑는다.
- 없애면 기존 마커를 "새 영상"으로 오인 → **AI 재호출 + 문서 통째 덮어쓰기**.
- 실측: 생성 후 수정된 마커가 **181개**(관리자 편집 포함) → 덮어쓰면 손실.
- 절감량은 스캔당 ~700 읽기(하루 전체의 3% 미만)로 **위험 대비 이득이 없다.**

### 5-4. 확장성 결론
- **채널 수가 아니라 "새로 잡히는 영상 수"가 시간을 결정한다.**
- 채널 1,000개여도 기존 영상 유지면 1~2분. `DAILY_AI_CAP=500`(영상 수 상한)이 폭주를 막는다.
- **분할 실행은 현재 불필요.** 필요해지면 그때 재측정 후 판단.
- 신규 채널은 등록 즉시 그 채널만 스캔(크론과 무관) → 매일 많이 추가해도 크론 시간에 영향 없음.

---

## 6. 대시보드 / 사용량 (`src/lib/gcpMonitoring.js`)

- **하루 경계 = 태평양 자정**(= 실제 한도 리셋, 한국/태국 기준 오후 2~3시). 예전엔 UTC 자정이라 값이 안 맞았다.
- Firebase 콘솔 표시는 **지연**이 커서 실시간 판단에 쓰면 안 된다. GCP Monitoring 실측이 정확하다.
- 무료 한도: Firestore 읽기 5만 / 쓰기 2만 / 삭제 2만, YouTube 1만 유닛 (모두 태평양 자정 리셋).

---

## 7. 🔴 이번 세션 최대 실패 — 반드시 읽을 것

사용자가 **"니 말을 믿을 수가 없다"**고 했다. 정당한 지적이었다. 원인:

**측정하지 않고 단정한 뒤 사실처럼 말했다.**

| 내가 한 주장 | 실제 |
|---|---|
| "스크래핑 252MB가 병목" | **틀림.** 99MB / 3.6초. 샘플 5개 순차측정을 곱한 추정이었음 |
| "Gemini 배치 15초라 AI가 병목" | **틀림.** 반복측정 평균 4.6초. 15.4초는 과부하 시점 1회 예외값 |
| "3단계 getAll 은 중복이니 제거" | **틀림.** 안전장치였음. 제거했으면 관리자 편집 181건 손실 위험 |

또한 검증 중 **자동화 브라우저로 영상 카드를 클릭해 운영 마커 1개를 오탐 차단**시켰다(복구 완료).

**다음 세션이 지킬 규칙:**
1. 원인을 **단정하지 않는다.** "확인했다"와 "추정이다"를 명확히 구분해 말한다.
2. 성능 수치는 **반복 측정(n≥3)** 후에만 말한다. 1회 측정으로 결론 내지 않는다.
3. 코드 **삭제·단순화 제안 전에 "왜 이게 있는지"** 먼저 확인한다.
4. 틀렸으면 **사용자가 지적하기 전에 먼저** 정정한다.
5. 사용자는 검증할 수 없고 그대로 믿고 결정한다. 잘못된 확신은 곧 사용자의 손해다.

---

## 8. 미해결 / 다음에 볼 것

| 항목 | 상태 |
|---|---|
| 구글 서치콘솔 색인 | "발견됨 - 색인 생성 안 됨" 45페이지. 신규 도메인이라 대기 중. 홈에서 SEO 페이지로 가는 **크롤 가능한 링크가 0개**인 게 약점(지도가 JS라). 필요 시 푸터/홈에 지역 링크 섹션 추가 검토 |
| 네이버 서치어드바이저 | robots.txt·사이트맵 등록 완료, 색인 대기 |
| `/api/markers/cities` | 무캐시 markers 전체 스캔 + **인증 없음**(관리자 폼 전용, 공개 링크 없음). 잠재 위험 |
| `/api/health` | 임시 진단용(코드에 "삭제 예정" 명시). 삭제 권장 |
| 홈 `getMapMarkers` | 유일하게 남은 렌더당 전체 읽기. 인덱스로 완화됨(2시간 캐시). 마커 3만 개 시 **페이지 payload** 자체가 문제 됨 → 그때 viewport 방식 검토 |
| 3D(Cesium) 검증 | 로컬 개발환경에 Cesium 정적 파일이 없어 **3D는 브라우저 검증 불가**. 3D 관련 변경은 사용자 육안 확인 필요 |
| `console.log` 정리 | `// TODO: 배포 전 제거` 다수 잔존 |

---

## 9. 검증 방법 메모

- 로컬: `.claude/launch.json` 의 `dev`(포트 3100). **프로덕션 Firestore 를 그대로 쓴다** → 쓰기 주의.
- 스캔 수동 실행: `GET /api/auto-channels/scan?secret=$REVALIDATE_SECRET` (로컬 서버로도 가능, 실제 DB 반영됨)
- 진단 스크립트는 **worktree 안에** 만들어야 `@/` 없이 `firebase-admin` import 가 된다. 쓰고 나면 **반드시 삭제**.
- 서비스 계정: `.env.local` 의 `FIREBASE_SERVICE_ACCOUNT_KEY` (JSON.parse + `private_key` 의 `\\n` → `\n` 치환 필요).

---

## 10. 관련 메모리 파일

`~/.claude/projects/C--Users-jeonghoon-lee-Desktop-Project-livecam-map/memory/`

- `auto-publish-to-master` — 매 작업 후 master 푸시
- `never-fabricate-verify-first` — 지어내지 말 것
- `no-video-card-click-in-automation` — 자동화에서 영상 카드 클릭 금지
- `youtube-quota-firestore-snapshot` — 할당량 사고 전체 이력 (§3 상세판)
- `i18n-architecture` — 17개 언어 구조
- `auto-marker-channel-system` — 자동채널 시스템
- `youtube-api-no-sharding` — 다중 키 금지
- `flag-api-costs-immediately` — 비용 발생 즉시 보고
